# ☁️ Cloudflare Pages 部署配置

## 项目信息
- **GitHub 仓库**: https://github.com/duliangkuan/zhanshi
- **项目类型**: Vue 3 + Vite

---

## 🔧 Cloudflare Pages 配置

### 基本设置
```
Project name: zhanshi
Production branch: main
```

### 构建设置
```
Framework preset: Vite
Build command: npm run build
Build output directory: dist
Root directory: / (留空或使用默认值)
Node.js version: 18 或更高（通常自动检测）
```

### 环境变量（如果需要）
目前项目不需要额外的环境变量

---

## 📋 部署步骤

### 通过网页界面部署

1. **访问 Cloudflare Dashboard**
   - https://dash.cloudflare.com/sign-up
   - 注册或登录账号

2. **创建 Pages 项目**
   - 点击 "Workers & Pages"
   - 点击 "Create application"
   - 选择 "Pages" → "Connect to Git"

3. **连接 GitHub**
   - 授权 Cloudflare 访问 GitHub
   - 选择仓库：`duliangkuan/zhanshi`
   - 点击 "Begin setup"

4. **配置项目**
   - 复制上面的配置信息填入
   - 点击 "Save and Deploy"

5. **等待部署完成**
   - 通常需要 1-3 分钟
   - 完成后获得 `.pages.dev` 域名

---

## 🎯 部署后

### 自动部署
每次推送代码到 GitHub 的 `main` 分支，Cloudflare Pages 会自动：
1. 检测到代码变更
2. 自动构建
3. 自动部署
4. 更新网站

### 查看部署状态
- 在 Cloudflare Dashboard 的项目页面可以看到：
  - 部署历史
  - 构建日志
  - 访问统计

### 自定义域名（可选）
1. 在项目设置中点击 "Custom domains"
2. 添加你的域名
3. 配置 DNS（如果域名在 Cloudflare，会自动配置）

---

## 🌍 访问网站

部署成功后，你的网站将通过以下方式访问：

- **Cloudflare Pages**: `https://zhanshi.pages.dev`（国内外访问都快）
- **Vercel**: `https://education1-obtp3svl6-duliangkuans-projects.vercel.app`（已部署）

推荐分享 Cloudflare Pages 的链接给国内用户！

---

## ❓ 常见问题

### Q: 构建失败怎么办？
A: 检查构建日志，通常是依赖问题。确保：
- `package.json` 中的依赖完整
- Node.js 版本正确（18+）

### Q: 页面显示 404？
A: 检查：
- Build output directory 是否设置为 `dist`
- 构建是否成功生成了 `dist` 目录

### Q: 国内访问还是慢？
A: Cloudflare Pages 已经是对国内较友好的方案。如果还需要更快：
- 考虑绑定自定义域名
- 或使用国内 CDN

---

## 📞 需要帮助？

如果在部署过程中遇到任何问题，可以：
1. 查看 Cloudflare Pages 文档：https://developers.cloudflare.com/pages/
2. 检查构建日志中的错误信息
3. 确保本地 `npm run build` 能成功运行

---

**祝部署顺利！** 🎉

